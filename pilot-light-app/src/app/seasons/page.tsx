import ComingNext from "@/components/ComingNext";

export default function Page() {
  return (
    <ComingNext
      number="03"
      title="Season Heatmap"
      blurb="Every show on the rail, every season, in one grid. The view only episodic media gives you. Up next."
    />
  );
}
